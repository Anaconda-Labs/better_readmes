name: Quality Check

on:
  push:
    branches: [main]
    paths:
      - 'README.md'
      - '*environment.yml'
      - '**/*environment.yml'
      - 'pixi.toml'
      - 'LICENSE'
  pull_request:
    branches: [main]

jobs:
  quality-check:
    uses: Anaconda-Labs/criteria_checker/.github/workflows/quality-check-callable.yml@f34fb2e6f8c7678471c1759029ca6fe32460ad82 # main
    with:
      resource_type: tell
      criteria_checker_owner: Anaconda-Labs
