name: readable-readmes
channels:
  - defaults
dependencies:
  # The link checker uses only the Python standard library, so no third
  # party packages are required. Python from the defaults channel is enough.
  # Python 3.10 or later is required to run the link checker, so we specify Python 3.13 here.
  - python=3.13.*
